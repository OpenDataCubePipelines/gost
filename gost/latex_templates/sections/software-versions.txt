% arara: pdflatex
% arara: pdflatex
% arara: clean: {{files: [{basename}.aux, {basename}.lof, {basename}.lot, {basename}.toc]}}
% Python string formatting template
\documentclass[../{main_doc}]{{subfiles}}

\begin{{document}}

  \begin{{flushleft}}
    The software versions listed are those found within the \textit{{*.proc-info.yaml}} file. 
  \end{{flushleft}}

  \subfile{{../tables/{basename}}}

\end{{document}}
